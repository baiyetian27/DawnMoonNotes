# 曦月笔记 — 开发阶段清单

> 每个阶段完成后勾选对应项。每个阶段的验证全部通过后才进入下一阶段。

---

## 第 0 阶段：项目基础设施 ✅

- [x] 创建 `docs/` 和 `开发日志/` 文件夹
- [x] 编写 `需求规格.md`
- [x] 编写 `技术架构.md`
- [x] 编写 `设计规范.md`
- [x] 编写 `数据库设计.md`
- [x] 编写 `开发阶段.md`（本文件）
- [x] 编写 `部署指南.md`
- [x] 创建第一篇开发日志
- [x] 创建项目根目录 `CLAUDE.md`

---

## 第 1 阶段：项目脚手架 + 空页面路由 ✅

**目标**：项目跑起来，能看到紫色主题的空页面框架。

- [x] 1.1 `npm create vite` 创建 React + TypeScript 项目
- [x] 1.2 安装 Tailwind CSS v4，配置紫色科技风主题色
- [x] 1.3 安装 React Router，创建所有空页面组件 + 路由配置
- [x] 1.4 搭建底部导航栏（笔记/曦筑/图谱/日历/随机）
- [x] 1.5 配置 PWA manifest + 基础图标 + Service Worker

**验证**：
- `npm run dev` 能启动
- 页面背景深色、紫色主题
- 手动改 URL 能切换页面
- 底部导航点击可跳转
- Chrome 地址栏出现 PWA 安装按钮

---

## 第 2 阶段：本地数据库 + 笔记基础功能 ✅

**目标**：打开即用，能新建笔记、编辑保存、列表查看、删除。

- [x] 2.1 安装 Dexie.js，定义 IndexedDB 数据模型（`src/lib/db.ts`）
- [x] 2.2 实现笔记 CRUD Service（本地存储）
- [x] 2.3 实现标签 Service + 链接 Service + 图片 Service
- [x] 2.4 实现笔记列表页（卡片布局，时间倒序，标签筛选）
- [x] 2.5 实现笔记编辑页（标题 + 副标题 + 正文 + 自动保存）
- [x] 2.6 实现删除笔记（带确认弹窗）
- [x] 2.7 实现笔记加锁/解锁功能
- [x] 2.8 实现笔记分享功能（图片 + 文本）
- [x] 2.9 创建设置页面（智能曦筑配置 + 数据导出/导入）
- [x] 2.10 更新所有技术文档

**验证**：
- 首次打开即可使用，无需登录
- 新建笔记 → 列表中显示 → 点击编辑 → 自动保存
- 刷新页面数据仍在
- 加锁后不可编辑，解锁后可编辑
- 分享功能可用（Android PWA 弹出系统分享菜单）
- 导出 → 清除数据 → 导入 → 数据恢复

---

## 第 3 阶段：富文本编辑器 ✅

**目标**：TipTap 集成，所有格式按钮可用，图片可插入。

- [x] 3.1 集成 TipTap，替换纯文本框
- [x] 3.2 加粗、斜体、下划线、删除线
- [x] 3.3 字号选择器（10-60）
- [x] 3.4 颜色按钮（7 色）
- [x] 3.5 对齐按钮（左/中/右）
- [x] 3.6 有序列表（自动编号）
- [x] 3.7 待办事项（可勾选）
- [x] 3.8 插入表格
- [x] 3.9 插入图片（本地 base64 存储）

**验证**：
- 逐一测试每个格式按钮
- 图片插入后正常显示
- 刷新后格式保留

---

## 第 4 阶段：标签系统 + 智能曦筑设置 ✅

**目标**：标签完整功能。

- [x] 4.1 标签管理页（创建/编辑/删除，7色可选）
- [x] 4.2 笔记编辑时选择标签
- [x] 4.3 笔记列表按标签筛选
- [x] 4.4 设置页：智能曦筑标签选择（最多 3 个，已完成）

**验证**：
- 创建/编辑/删除标签正常
- 标签筛选正确
- 智能曦筑设置保存后生效

---

## 第 5 阶段：曦筑 ✅

**目标**：笔记可置顶、曦筑仪表板可用。

- [x] 5.1 笔记置顶/取消置顶功能（已完成）
- [x] 5.2 曦筑仪表板页面（卡片布局）
- [x] 5.3 卡片内联待办勾选
- [x] 5.4 智能曦筑合并（手动 + 标签规则）

**验证**：
- 置顶笔记排最前 + 视觉区分
- 仪表板卡片正确展示
- 待办内联勾选同步

---

## 第 6 阶段：双向链接 ✅

**目标**：`@` 语法创建链接 + 反向链接面板。

- [x] 6.1 TipTap `@` 提及扩展
- [x] 6.2 选择笔记后插入可点击链接
- [x] 6.3 保存时解析链接 → 写入 links 表
- [x] 6.4 笔记页底部：反向链接面板

**验证**：
- 输入 @ 弹窗显示笔记标题
- 选择后插入链接
- 目标笔记出现反向链接

---

## 第 7 阶段：知识图谱 ✅

**目标**：力导向图可视化笔记关联。

- [x] 7.1 读取笔记 + 链接数据构建节点/边
- [x] 7.2 react-force-graph-2d 渲染图谱
- [x] 7.3 核心节点更大更亮
- [x] 7.4 标签筛选
- [x] 7.5 点击节点跳转

**验证**：
- 图谱正确显示笔记和连线
- 筛选功能正常
- 点击跳转正常

---

## 第 8 阶段：日历 + 随机回顾 ✅

**目标**：日历查看 + 随机漫步。

- [x] 8.1 自定义月历组件
- [x] 8.2 点击日期查看当天笔记
- [x] 8.3 随机漫步：随机抽取笔记闪卡展示
- [x] 8.4 标签筛选随机范围

**验证**：
- 日历格子正确显示
- 随机漫步每次不同

---

## 第 9 阶段：搜索 + PWA + 部署 ✅

**目标**：搜索可用、离线可用、公网可访问。

- [x] 9.1 搜索页面 + 搜索逻辑
- [x] 9.2 结果高亮 + 点击跳转
- [x] 9.3 PWA 离线缓存策略
- [x] 9.4 响应式适配收尾
- [x] 9.5 部署到 Vercel（vercel.json 已配置）
- [ ] 9.6 安卓 Chrome 安装 PWA 全流程测试（待用户部署后验证）

**验证**：
- 搜索可找到笔记
- 断网可查看已有笔记
- 公网 URL 可访问
- 手机端全功能正常

---

## 里程碑

| 里程碑 | 对应阶段 | 可体验的功能 |
|--------|----------|-------------|
| M1 | 第 1 阶段后 | 看到紫色主题的空 App 框架 |
| M2 | 第 2 阶段后 | 笔记 CRUD + 加锁 + 分享 + 数据迁移 |
| M3 | 第 3 阶段后 | 完整的富文本笔记编辑器 |
| M4 | 第 6 阶段后 | 双向链接（核心差异功能） |
| M5 | 第 7 阶段后 | 知识图谱可视化 |
| M6 | 第 9 阶段后 | 全功能上线，手机可用 ✅ |
